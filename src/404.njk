---
layout: base.njk
title: 404 - Page Not Found
description: The page you're looking for doesn't exist.
permalink: /404.html
eleventyExcludeFromCollections: true
---

<section class="error-page">
    <div class="container container--narrow">
        <div class="error-content">
            <!-- Large 404 with Bauhaus styling -->
            <div class="error-number">
                <span class="error-digit error-digit--4">4</span>
                <div class="error-digit error-digit--0">
                    <div class="geometric-zero">
                        <div class="circle-outer"></div>
                        <div class="circle-inner"></div>
                    </div>
                </div>
                <span class="error-digit error-digit--4">4</span>
            </div>
            
            <!-- Geometric decorations -->
            <div class="error-geometric" aria-hidden="true">
                <div class="shape shape--circle"></div>
                <div class="shape shape--square"></div>
                <div class="shape shape--triangle"></div>
            </div>
            
            <h1 class="error-title">Page Not Found</h1>
            <p class="error-message">
                The page you're looking for doesn't exist or has been moved.
            </p>
            
            <div class="error-actions">
                <a href="{{ '/' | baseUrl }}" class="btn btn--primary">
                    ← Back to Home
                </a>
                <a href="{{ '/projects/' | baseUrl }}" class="btn btn--outline">
                    View Projects
                </a>
            </div>
            
            <!-- Helpful links -->
            <div class="error-links">
                <h2 class="error-links-title">You might be looking for:</h2>
                <ul class="error-links-list">
                    <li><a href="{{ '/' | baseUrl }}">Home</a></li>
                    <li><a href="{{ '/projects/' | baseUrl }}">Projects</a></li>
                    <li><a href="{{ '/blog/' | baseUrl }}">Blog</a></li>
                    <li><a href="{{ '/about/' | baseUrl }}">About</a></li>
                    <li><a href="{{ '/contact/' | baseUrl }}">Contact</a></li>
                </ul>
            </div>
        </div>
    </div>
</section>

<style>
/* 404 Error Page Styles */
.error-page {
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-3xl) 0;
    position: relative;
    overflow: hidden;
}

.error-content {
    text-align: center;
    position: relative;
    z-index: 1;
}

/* Large 404 Number */
.error-number {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-2xl);
    font-size: 8rem;
    font-weight: 900;
    line-height: 1;
}

.error-digit {
    color: var(--color-primary);
}

.error-digit--0 {
    position: relative;
}

.geometric-zero {
    width: 120px;
    height: 160px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
}

.circle-outer {
    width: 120px;
    height: 120px;
    border: 16px solid var(--color-secondary);
    border-radius: 50%;
    position: absolute;
}

.circle-inner {
    width: 60px;
    height: 60px;
    background: var(--color-background);
    border-radius: 50%;
    position: absolute;
}

/* Geometric Decorations */
.error-geometric {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    z-index: -1;
}

.error-geometric .shape {
    position: absolute;
    opacity: 0.15;
}

.error-geometric .shape--circle {
    width: 200px;
    height: 200px;
    background: var(--color-primary);
    border-radius: 50%;
    top: 10%;
    left: 10%;
    animation: float 8s ease-in-out infinite;
}

.error-geometric .shape--square {
    width: 150px;
    height: 150px;
    background: var(--color-secondary);
    top: 60%;
    right: 15%;
    transform: rotate(45deg);
    animation: rotate 12s linear infinite;
}

.error-geometric .shape--triangle {
    width: 0;
    height: 0;
    border-left: 100px solid transparent;
    border-right: 100px solid transparent;
    border-bottom: 173px solid var(--color-accent);
    bottom: 10%;
    left: 20%;
    animation: pulse 6s ease-in-out infinite;
}

/* Text Content */
.error-title {
    font-size: var(--font-size-4xl);
    font-weight: 900;
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
}

.error-message {
    font-size: var(--font-size-xl);
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-2xl);
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
}

/* Actions */
.error-actions {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    margin-bottom: var(--spacing-3xl);
}

/* Helpful Links */
.error-links {
    padding-top: var(--spacing-2xl);
    border-top: 2px solid var(--color-border);
    max-width: 400px;
    margin: 0 auto;
}

.error-links-title {
    font-size: var(--font-size-lg);
    font-weight: 600;
    margin-bottom: var(--spacing-lg);
    color: var(--color-text-secondary);
}

.error-links-list {
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-md);
    justify-content: center;
}

.error-links-list li {
    display: inline-block;
}

.error-links-list a {
    display: inline-block;
    padding: var(--spacing-sm) var(--spacing-lg);
    background: var(--color-surface-raised);
    border-radius: 0;
    text-decoration: none;
    color: var(--color-text);
    font-weight: 500;
    transition: all var(--transition-base);
}

.error-links-list a:hover {
    background: var(--color-primary);
    color: white;
    transform: translateY(-2px);
}

/* Responsive */
@media (max-width: 768px) {
    .error-number {
        font-size: 5rem;
        gap: var(--spacing-md);
    }
    
    .geometric-zero {
        width: 80px;
        height: 100px;
    }
    
    .circle-outer {
        width: 80px;
        height: 80px;
        border-width: 12px;
    }
    
    .circle-inner {
        width: 40px;
        height: 40px;
    }
    
    .error-title {
        font-size: var(--font-size-3xl);
    }
    
    .error-message {
        font-size: var(--font-size-lg);
    }
    
    .error-actions {
        flex-direction: column;
        max-width: 300px;
        margin-left: auto;
        margin-right: auto;
    }
    
    .error-actions .btn {
        width: 100%;
    }
}

/* Reduce motion */
@media (prefers-reduced-motion: reduce) {
    .error-geometric .shape {
        animation: none;
    }
}
</style>
