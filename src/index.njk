---
layout: base.njk
title: Home
description: Discover the Bauhaus movement through a clean, modern lens - geometric design principles for the digital age
---

<!-- Hero Section -->
<section class="hero">
    <div class="container">
        <div class="hero-content">
            <h1 class="hero-title">
                Bauhaus Revival
            </h1>
            
            <p class="hero-subtitle">
                Where geometric purity meets modern simplicity. Form follows function in the digital age.
            </p>
            
            <div class="hero-cta">
                <a href="{{ '/projects/' | baseUrl }}" class="btn btn--primary btn--large">
                    Learn More
                </a>
                <a href="{{ '/contact/' | baseUrl }}" class="btn btn--secondary btn--large">
                    Get Started
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Core Principles Section -->
<section class="principles section">
    <div class="container">
        <h2 class="section-title">Core Principles</h2>
        
        <div class="principles-grid">
            <div class="principle-card">
                <div class="principle-icon">○</div>
                <h3>Form Follows Function</h3>
                <p>Design must serve purpose. Every element justified by its utility and necessity.</p>
            </div>
            
            <div class="principle-card">
                <div class="principle-icon">□</div>
                <h3>Geometric Harmony</h3>
                <p>Primary shapes create universal language. Circle, square, triangle define balance.</p>
            </div>
            
            <div class="principle-card">
                <div class="principle-icon">△</div>
                <h3>Unity of Design</h3>
                <p>Breaking barriers between art and craft. One cohesive philosophy across disciplines.</p>
            </div>
        </div>
    </div>
</section>

<!-- Featured Projects Section -->
<section class="featured section section--light">
    <div class="container">
        <h2 class="section-title">Explore the Movement</h2>
        
        <div class="featured-grid">
            {% set featuredProjects = collections.projects | filter('featured') | limit(3) %}
            {% if featuredProjects.length > 0 %}
                {% for project in featuredProjects %}
                <a href="{{ project.url | baseUrl }}" class="featured-card">
                    {% if project.data.image %}
                    <div class="featured-image">
                        <img src="{{ project.data.image | baseUrl }}" 
                             alt="{{ project.data.title }}"
                             loading="lazy">
                    </div>
                    {% endif %}
                    <div class="featured-info">
                        <h3>{{ project.data.title }}</h3>
                        <p>{{ project.data.description or (project.templateContent | excerpt) }}</p>
                    </div>
                </a>
                {% endfor %}
            {% else %}
                <a href="#" class="featured-card">
                    <div class="featured-info">
                        <h3>Form Follows Function</h3>
                        <p>Design serving purpose above all else.</p>
                    </div>
                </a>
                <a href="#" class="featured-card">
                    <div class="featured-info">
                        <h3>Geometric Abstraction</h3>
                        <p>Primary shapes as universal language.</p>
                    </div>
                </a>
                <a href="#" class="featured-card">
                    <div class="featured-info">
                        <h3>Unity of Arts</h3>
                        <p>Dissolving barriers between disciplines.</p>
                    </div>
                </a>
            {% endif %}
        </div>
    </div>
</section>

<!-- Blog Timeline Section -->
<section class="timeline section">
    <div class="container">
        <h2 class="section-title">Historical Journey</h2>
        
        <div class="timeline-container">
            {% set recentPosts = collections.blog | reverse | limit(3) %}
            {% if recentPosts.length > 0 %}
                {% for post in recentPosts %}
                <a href="{{ post.url | baseUrl }}" class="timeline-item">
                    <div class="timeline-date">
                        {{ post.data.date | dateFormat('YYYY') }}
                    </div>
                    <div class="timeline-content">
                        <h3>{{ post.data.title }}</h3>
                        <p>{{ post.data.description or (post.templateContent | excerpt) }}</p>
                    </div>
                </a>
                {% endfor %}
            {% else %}
                <a href="#" class="timeline-item">
                    <div class="timeline-date">1919</div>
                    <div class="timeline-content">
                        <h3>The Weimar Years</h3>
                        <p>Founded by Walter Gropius, revolutionizing art education.</p>
                    </div>
                </a>
                <a href="#" class="timeline-item">
                    <div class="timeline-date">1925</div>
                    <div class="timeline-content">
                        <h3>Dessau Era</h3>
                        <p>Most productive period producing iconic furniture and design.</p>
                    </div>
                </a>
                <a href="#" class="timeline-item">
                    <div class="timeline-date">1933+</div>
                    <div class="timeline-content">
                        <h3>Global Legacy</h3>
                        <p>Bauhaus principles spread worldwide, shaping modernism forever.</p>
                    </div>
                </a>
            {% endif %}
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="cta-final section section--accent">
    <div class="container">
        <h2>Ready to Explore?</h2>
        <p>Join us in discovering how Bauhaus shapes modern design today.</p>
        <div class="cta-buttons">
            <a href="{{ '/projects/' | baseUrl }}" class="btn btn--light btn--large">
                View All Projects
            </a>
            <a href="{{ '/contact/' | baseUrl }}" class="btn btn--outline-light btn--large">
                Get in Touch
            </a>
        </div>
    </div>
</section>