---
layout: base.njk
title: Home
description: Experience Humanist Modernism through digital design - prioritizing human readability, warmth, and accessibility over cold geometric ideals. Exploring Adrian Frutiger and Edward Tufte's principles.
---

<!-- Hero Section -->
<section class="hero">
    <div class="container">
        <div class="hero-content">
            <!-- Bauhaus geometric decorative elements removed for Humanist Modernism -->
            <!-- HM focuses on content and readability over decorative geometry -->
            
            <h1 class="hero-title">
                Humanist Modernism: Clarity Serving Humanity
            </h1>
            
            <p class="hero-subtitle">
                Design is how it works. Prioritizing human readability, warmth, and accessibility over cold geometric ideals.
            </p>
            
            <div class="hero-cta">
                <a href="{{ '/projects/' | baseUrl }}" class="btn btn--primary">
                    See Key Typeface Examples
                    <span aria-hidden="true">→</span>
                </a>
                <a href="{{ '/about/' | baseUrl }}" class="btn btn--outline">
                    Read the Core Principles
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Featured Projects Section -->
<section class="featured-projects section">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Foundational Principles</h2>
            <p class="section-subtitle">
                The design concepts that prioritize legibility, empathy, and the user experience
            </p>
        </div>
        
        <div class="grid grid--3-col">
            {% set featuredProjects = collections.projects | filter('featured') | limit(3) %}
            {% if featuredProjects.length > 0 %}
                {% for project in featuredProjects %}
                <article class="card project-card card-geometric">
                    {% if project.data.image %}
                    <div class="card-image">
                        <img src="{{ project.data.image | baseUrl }}" 
                             alt="{{ project.data.title }}"
                             loading="lazy">
                    </div>
                    {% endif %}
                    
                    <div class="card-content">
                        <h3 class="card-title">
                            <a href="{{ project.url | baseUrl }}">
                                {{ project.data.title }}
                            </a>
                        </h3>
                        
                        <p class="card-description">
                            {{ project.data.description or (project.templateContent | excerpt) }}
                        </p>
                        
                        {% if project.data.tags %}
                        <div class="card-tags">
                            {% for tag in project.data.tags %}
                            {% if tag != "projects" %}
                            <span class="tag">{{ tag }}</span>
                            {% endif %}
                            {% endfor %}
                        </div>
                        {% endif %}
                        
                        {% if project.data.status %}
                        <span class="project-status status-{{ project.data.status }}">
                            {{ project.data.status }}
                        </span>
                        {% endif %}
                    </div>
                    
                    <div class="card-footer">
                        <a href="{{ project.url | baseUrl }}" class="card-link">
                            Explore Design Element <span aria-hidden="true">→</span>
                        </a>
                    </div>
                </article>
                {% endfor %}
            {% else %}
                <!-- Placeholder content aligned with Humanist Modernism -->
                <article class="card project-card card-geometric">
                    <div class="card-content">
                        <h3 class="card-title">Readability Paramount</h3>
                        <p class="card-description">Legibility is the cornerstone. Generous body text and ample line-height serve the reader first.</p>
                        <div class="card-tags">
                            <span class="tag">Typography</span>
                            <span class="tag">Readability</span>
                        </div>
                    </div>
                </article>
                <article class="card project-card card-geometric">
                    <div class="card-content">
                        <h3 class="card-title">Empathetic Typography</h3>
                        <p class="card-description">Emphasis on humanist sans-serifs (Frutiger, Meta) with open apertures and varied stroke width for warmth.</p>
                        <div class="card-tags">
                            <span class="tag">Typefaces</span>
                            <span class="tag">Humanist</span>
                        </div>
                    </div>
                </article>
                <article class="card project-card card-geometric">
                    <div class="card-content">
                        <h3 class="card-title">Content-First Layout</h3>
                        <p class="card-description">Prioritizing the reading experience with ample white space, narrow measures (720px max-width), and clear hierarchy.</p>
                        <div class="card-tags">
                            <span class="tag">Layout</span>
                            <span class="tag">Accessibility</span>
                        </div>
                    </div>
                </article>
            {% endif %}
        </div>
        
        <div class="section-footer">
            <a href="{{ '/projects/' | baseUrl }}" class="btn btn--secondary">
                View All Principles
                <span aria-hidden="true">→</span>
            </a>
        </div>
    </div>
</section>

<!-- Recent Blog Posts Section -->
<section class="recent-posts section section--alt">
    <div class="container">
        <div class="section-header">
            <h2 class="section-title">Key Practitioners & Context</h2>
            <p class="section-subtitle">
                Understanding the figures who championed design that serves human needs
            </p>
        </div>
        
        <div class="grid grid--3-col">
            {% set recentPosts = collections.blog | reverse | limit(3) %}
            {% if recentPosts.length > 0 %}
                {% for post in recentPosts %}
                <article class="card post-card">
                    <div class="card-content">
                        {% if post.data.date %}
                        <time class="card-date post-date" datetime="{{ post.data.date | dateFormat('YYYY-MM-DD') }}">
                            {{ post.data.date | dateFormat('MMMM D, YYYY') }}
                        </time>
                        {% endif %}
                        
                        <h3 class="card-title">
                            <a href="{{ post.url | baseUrl }}">
                                {{ post.data.title }}
                            </a>
                        </h3>
                        
                        <p class="card-description">
                            {{ post.data.description or (post.templateContent | excerpt) }}
                        </p>
                        
                        {% if post.data.tags %}
                        <div class="card-tags">
                            {% for tag in post.data.tags %}
                            {% if tag != "blog" %}
                            <span class="tag">{{ tag }}</span>
                            {% endif %}
                            {% endfor %}
                        </div>
                        {% endif %}
                    </div>
                    
                    <div class="card-footer">
                        <a href="{{ post.url | baseUrl }}" class="card-link">
                            Read More <span aria-hidden="true">→</span>
                        </a>
                        {% if post.templateContent %}
                        <span class="reading-time">{{ post.templateContent | readingTime }} min read</span>
                        {% endif %}
                    </div>
                </article>
                {% endfor %}
            {% else %}
                <!-- Placeholder content featuring Humanist Modernism key figures -->
                <article class="card post-card">
                    <div class="card-content">
                        <time class="card-date post-date">1954-1967</time>
                        <h3 class="card-title">Adrian Frutiger & Univers</h3>
                        <p class="card-description">The humanist reaction to geometric sans-serifs, creating readable and functional type systems that prioritize human perception and warmth.</p>
                        <div class="card-tags">
                            <span class="tag">Frutiger</span>
                            <span class="tag">Typography</span>
                        </div>
                    </div>
                </article>
                <article class="card post-card">
                    <div class="card-content">
                        <time class="card-date post-date">1983-Present</time>
                        <h3 class="card-title">Edward Tufte & Information Design</h3>
                        <p class="card-description">Principles of visual display: maximizing the data-ink ratio while insisting on clear, efficient communication that respects the viewer's intelligence.</p>
                        <div class="card-tags">
                            <span class="tag">Tufte</span>
                            <span class="tag">Data Viz</span>
                        </div>
                    </div>
                </article>
                <article class="card post-card">
                    <div class="card-content">
                        <time class="card-date post-date">2000-Present</time>
                        <h3 class="card-title">The UX Movement</h3>
                        <p class="card-description">Influencing user experience, mobile-first design, and accessibility (WCAG), making digital design truly universal and human-centered.</p>
                        <div class="card-tags">
                            <span class="tag">Accessibility</span>
                            <span class="tag">Mobile-First</span>
                        </div>
                    </div>
                </article>
            {% endif %}
        </div>
        
        <div class="section-footer">
            <a href="{{ '/blog/' | baseUrl }}" class="btn btn--secondary">
                View All Articles
                <span aria-hidden="true">→</span>
            </a>
        </div>
    </div>
</section>

<!-- Call to Action Section -->
<section class="cta-section section">
    <div class="container">
        <div class="cta-content">
            <h2 class="cta-title">Design that Works for Humans</h2>
            <p class="cta-text">
                Explore how Humanist Modernism applies clarity and warmth to solve complex problems in information architecture and digital product design.
            </p>
            
            <div class="cta-actions">
                <a href="{{ '/contact/' | baseUrl }}" class="btn btn--accent btn--lg">
                    Apply the Principles
                    <span aria-hidden="true">→</span>
                </a>
            </div>
        </div>
    </div>
</section>