---
layout: base.njk
title: Blog
description: Articles about web design, development, and the creative process.
---

<section class="page-header">
    <div class="container">
        <h1 class="page-title">Blog</h1>
        <p class="page-subtitle">
            Thoughts on design, development, and creativity
        </p>
    </div>
</section>

<section class="blog-list section">
    <div class="container">
        {% if collections.blog.length > 0 %}
        <div class="grid grid--2-col">
            {% for post in collections.blog | reverse %}
            <article class="card post-card">
                <div class="card-content">
                    {% if post.data.date %}
                    <time class="card-date" datetime="{{ post.data.date | dateISO }}">
                        {{ post.data.date | dateFormat }}
                    </time>
                    {% endif %}
                    
                    <h2 class="card-title">
                        <a href="{{ post.url | baseUrl }}">
                            {{ post.data.title }}
                        </a>
                    </h2>
                    
                    <p class="card-description">
                        {{ post.data.description or (post.templateContent | excerpt) }}
                    </p>
                    
                    {% if post.data.tags %}
                    <div class="card-tags">
                        {% for tag in post.data.tags %}
                        {% if tag != "blog" %}
                        <span class="tag">{{ tag }}</span>
                        {% endif %}
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>
                
                <div class="card-footer">
                    <a href="{{ post.url | baseUrl }}" class="card-link">
                        Read More <span aria-hidden="true">→</span>
                    </a>
                    {% if post.templateContent %}
                    <span class="reading-time">{{ post.templateContent | readingTime }} min read</span>
                    {% endif %}
                </div>
            </article>
            {% endfor %}
        </div>
        {% else %}
        <div class="empty-state">
            <p>No blog posts yet. Check back soon!</p>
        </div>
        {% endif %}
    </div>
</section>
