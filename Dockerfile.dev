# Development Dockerfile for Bauhaus Revival Portfolio
FROM node:18-alpine

# Set working directory
WORKDIR /app

# Install dependencies first for better caching
COPY package*.json ./
RUN npm install

# Copy source files
COPY . .

# Expose port for development server
EXPOSE 8080

# Start development server with live reload
CMD ["npm", "run", "dev"]
